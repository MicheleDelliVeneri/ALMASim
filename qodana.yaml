version: "1.0"
linter: jetbrains/qodana-python:2024.2
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
exclude:
  - name: OldFiles
    paths:
    - old-version/**
    - tests/**
    - "*.pyc"